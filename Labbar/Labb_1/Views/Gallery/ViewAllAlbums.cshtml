@model IList<Labb_1.Models.Album>
<a id="CloseAlbums" href="@Url.Action("Index","Gallery")"><h1>X</h1></a>
<div id="Albumflexcontainer">
    <br />
    <br />
    <div class="row">
       
    </div>
    <br />
    <br />
    <div class="row">
        @foreach (var album in Model)
            {
            <div class="Album-Thumb">
                <a href='@Url.Action("ViewAlbum", "Album", new {albumId = album.AlbumId}, null)'>@album.Name</a>
            </div>
        }
    </div>
</div>
<link href="~/Content/ViewAlbums.css" rel="stylesheet" />